<h1><%= @user.username.capitalize %>'s profile page</h1>

<% if logged_in? && @user == current_user %>
  Here are your current posts:
<% else %>
  Here are their current posts:
<% end %>


<% @user.posts.each do |post| %>
  <br><a href="/posts/<%= post.id %>"><%= post.title %></a><br>
<% end %>
